import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import GeneratorCalculator from './GeneratorCalculator';

ReactDOM.render(
  <React.StrictMode>
    <GeneratorCalculator />
  </React.StrictMode>,
  document.getElementById('root')
);
